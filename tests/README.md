# Tests

This directory is reserved for focused regression tests around tokenization, trie lookup, posting-list behavior, BM25 ranking, and CLI workflows.

No automated test framework is configured yet.
